[project]
name = "ml-interview-notes"
version = "0.1.0"
description = "Executable ML notes for interview preparation"
requires-python = ">=3.11"
dependencies = [
  "ipykernel>=6.29",
  "jupyter>=1.1",
  "matplotlib>=3.9",
  "numpy>=2.0",
  "pyyaml>=6.0.3",
  "torch>=2.4",
]
